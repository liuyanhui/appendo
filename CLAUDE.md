# CLAUDE.md - 软件开发项目规范

本文用来定义本项目的开发规范，包括：目录结构、开发流程、文件维护规则等。

---

## 📁 项目目录

```
CLAUDE.md                 # 项目规则和开发指南
docs/                     # 项目文档目录
├── specs/                # 需求文档目录
├── design/               # 设计文档目录
│   └── DESIGN.md         # 系统设计文档（架构、模块、接口等）
├── plans/                # 项目计划文档目录
│   ├── PLANS.md          # 项目计划（里程碑、排期等）
│   ├── active/           # 进行中的执行计划目录
│   ├── completed/        # 已完成的执行计划目录
│   └── debt-tracker.md   # 技术债务追踪
├── background/           # 参考资料（技术选型依据、竞品分析等）
└── README.md             # 项目说明
src/                      # 源代码目录
tests/                    # 测试目录
scripts/                  # 构建、部署等脚本目录
```

**目录维护规则**：
- 需求变更必须先更新需求文档，然后修改计划和设计，再按最新方案执行
- 优先从需求文档获取项目信息
- 需求文档与设计文档冲突时，以需求文档为准
- 设计文档是对需求文档的技术具体化
- 任何 `CLAUDE.md` 的更新需经过用户同意
- 不要混淆需求文档和设计文档

**需求变更处理流程**：
- 涉及需求变化时，必须先同步更新 `docs/specs.md`（追加方式）
- specs.md 更新后，等待用户确认需求变更
- 用户确认后，更新 `docs/design.md` 设计文档（追加方式）
- design.md 更新后，等待用户确认设计方案
- 用户确认后，才能修改代码实现

---

## 开发流程

**阶段1：需求收集**
- 理解项目的目标和功能范围
- 确认用户场景、业务约束、性能要求、安全要求等
- 确认技术栈偏好和第三方依赖限制
- 追加到 `docs/specs/` 目录文档（经用户确认）
- 需求不要轻易定稿。要努力思考是否有遗漏，如有遗漏需跟用户确认
- 启用subagent评估需求是否完备合理。

**阶段2：方案设计**
- 完成系统架构设计，保存到 `docs/design/DESIGN.md`
- 内容包括：模块划分、数据模型、接口设计、技术选型及理由
- 校验设计是否满足需求文档中的功能和约束
- 可设计多个方案供用户选择

**阶段3：制定执行计划**
- 根据 `docs/design/DESIGN.md` 完成执行计划，保存到 `docs/plans/PLANS.md`
- 计划包括：开发任务拆分、任务依赖关系、优先级、验收标准
- 计划要满足需求文档和设计文档的要求和约束
- 执行时以设计文档为主要参考，必要时可查阅需求文档澄清歧义

**阶段4：编码实现**
- 按执行计划逐步完成开发任务
- 遵循项目既有的代码风格和约定
- 编写必要的单元测试和集成测试
- 如需引入新依赖，需说明理由并与用户确认
- 遇到设计变更时，回到阶段2更新设计文档

**阶段5：测试与验收**
- 根据需求文档和设计文档验证实现
- 确保所有测试通过
- 如不满足需求，制定新计划，从阶段3循环执行

**阶段6：总结**
- 项目完成后，收集本次开发中的新规则和经验，更新到本文档对应章节
- 更新 `docs/plans/debt-tracker.md` 记录遗留的技术债务

**异常流程与回退机制**：
- **编码阶段发现需求不明确**：暂停编码，记录疑点，与用户确认后再继续，必要时回退到阶段1补充需求
- **编码阶段发现设计缺陷**：暂停编码，回退到阶段2更新设计文档，经用户确认后重新进入阶段3调整计划
- **测试阶段发现设计不合理**：记录问题，回退到阶段2修订设计，更新计划后重新执行
- **紧急 hotfix**：可跳过阶段1-3，直接编码修复，事后补充文档记录

---

## 🚫 禁止事项

- **禁止**在未确认需求的情况下开始编码实现
- **禁止**在目录中存放不属于该目录主题的内容（如非需求文档不得放入 `specs/`）
- **禁止**删除或覆盖需求文档中的历史记录和变更历史
- **禁止**需求变更后未更新需求文档就开始实施
- **禁止**跳过需求确认直接开始编码
- **禁止**流程每个阶段完成后未获用户确认就进入下一阶段
- **禁止**需求变更后未等用户确认就修改设计文档或代码
- **禁止**设计文档变更后未等用户确认就修改代码

---

## 文件维护规则

- 需求变更时以追加方式更新需求文档，并记录变更时间
- 只有用户明确要求重构时才重构需求文档
- 支持多个设计方案并行，通过文件名区分
- 文档头部应包含修订记录，记录每次变更的日期、变更人、变更摘要
- 文档内容过期时，标注过期标记并注明替代文档位置，不直接删除
- 执行计划完成后，从 `active/` 移动到 `completed/`，并在文件中记录完成日期

---

## 测试规范

- 测试文件放置在 `tests/` 目录，目录结构与 `src/` 对应
- 单元测试覆盖核心业务逻辑，最低覆盖率目标：60%
- 测试文件命名：`test_<模块名>.<ext>` 或 `<模块名>.test.<ext>`
- 测试内容应包括：正常路径、边界条件、异常输入
- 修复 bug 时，必须先编写能复现 bug 的测试，再编写修复代码
- 测试框架和工具的选择需与用户确认

---

## AI 协作规范

- 修改文件前，展示变更摘要，说明改了什么、为什么改
- 可以主动创建项目规范中定义的目录下的文件（如 `docs/`、`src/`、`tests/`）
- 创建项目规范之外的目录或文件，需先与用户确认
- 遇到不确定的需求或技术选型时，提供 2-3 个选项并说明各自优劣，由用户决策
- 不得假设或编造用户未提供的信息，不确定时应主动询问
- 一次对话中涉及多文件修改时，按逻辑分组展示，每组完成后等待用户确认再继续

---

## 系统权限

- 以下操作可直接执行，无需用户许可：创建/编辑源代码文件、创建/编辑文档、运行测试、执行构建脚本、查看 git 状态
- 以下操作需用户确认：删除文件、安装/卸载软件依赖、推送代码到远程仓库、修改 CI/CD 配置、修改环境配置文件（.env 等）、操作数据库
- 尽量不要中断任务，将需要确认的操作集中展示

---

## 其他规则
- 代码完成后要先补充单元测试，并测试。