# CLAUDE.md - 软件开发项目规范

本文用来定义本项目的开发规范，包括：目录结构、开发流程、文件维护规则等。

---

## 📁 项目目录

```
CLAUDE.md                 # 项目规则和开发指南
CODINGRULES.md            # 编码规范（测试、Bug修复、常量等）
AGENT.md                  # AI 代理入口（指向 CLAUDE.md）
README.md                 # 面向用户/贡献者的项目说明
CHANGELOG.md              # 版本变更记录
docs/                     # 项目文档目录
├── specs.md              # 需求文档（追加式变更史）
├── architecture.md       # 架构文档（现状快照，面向开发者的唯一权威源）
├── design.md             # 系统设计（历史变更史；现状见 architecture.md）
├── design/               # 特性设计文档（历史设计记录，含状态标注）
├── plans/                # 项目计划文档目录
│   ├── PLANS.md          # 项目计划（里程碑、排期等）
│   ├── active/           # 进行中的执行计划目录
│   ├── completed/        # 已完成的执行计划目录
│   └── debt-tracker.md   # 技术债务追踪
├── background/           # 参考资料（技术选型依据、竞品分析等）
└── directory-documentation-analysis.md  # 目录文档体系分析（历史）
Appendo/                  # Android 应用模块
├── src/main/             # 主源码与资源
└── src/test/             # 单元测试
gradle/                   # Gradle wrapper 与版本目录（libs.versions.toml）
```

**目录维护规则**：
- 需求变更必须先更新需求文档，然后修改计划和设计，再按最新方案执行
- 优先从需求文档获取项目信息
- 需求文档与设计文档冲突时，以需求文档为准
- 设计文档是对需求文档的技术具体化
- 任何 `CLAUDE.md` 的更新需经过用户同意
- 不要混淆需求文档和设计文档

**需求变更处理流程**：
- 涉及需求变化时，必须先同步更新 `docs/specs.md`（追加方式）
- specs.md 更新后，等待用户确认需求变更
- 用户确认后，更新 `docs/design.md` 设计文档（追加方式）
- design.md 更新后，等待用户确认设计方案
- 用户确认后，才能修改代码实现

---

## 👤 用户使用习惯

设计交互、数据处理（尤其是"内容是否原样保留 / 是否注入隐形标记"）、以及外部兼容性相关功能时，必须参考以下真实使用习惯：

1. **拷贝单条记录**到其他编辑器或微信
2. **拷贝全部记录**到其他编辑器或微信
3. **很少使用其他软件直接打开文件**

设计含义（须落实到方案）：
- **"复制 / 分享"是内容离开本应用的主要路径**——复制/分享出去的内容必须干净（无零宽空格等隐形字符、无内部标记外溢）。任何"写入时加标记"的方案，必须在所有复制/分享出口剥离标记。
- **"直接用其他软件打开文件"是低频场景**——对该场景的兼容性投入应降低优先级；可作"已知约束 / 不支持场景"处理（并在 README 注明），而非硬性保证。不应为该低频场景牺牲高频复制/分享路径的体验。

---

## 开发流程

**阶段1：需求收集**
- 理解项目的目标和功能范围
- 确认用户场景、业务约束、性能要求、安全要求等
- 确认技术栈偏好和第三方依赖限制
- 追加到 `docs/specs.md`（经用户确认）
- 需求不要轻易定稿。要努力思考是否有遗漏，如有遗漏需跟用户确认
- 启用subagent评估需求是否完备合理。

**阶段2：方案设计**
- 完成系统架构设计，保存到 `docs/design.md`（系统级追加）或 `docs/design/<特性>-design.md`（特性级新文档）
- 内容包括：模块划分、数据模型、接口设计、技术选型及理由
- 校验设计是否满足需求文档中的功能和约束
- 可设计多个方案供用户选择

**阶段3：制定执行计划**
- 根据设计文档（`docs/design.md` / `docs/design/`）完成执行计划，保存到 `docs/plans/PLANS.md`
- 计划包括：开发任务拆分、任务依赖关系、优先级、验收标准
- 计划要满足需求文档和设计文档的要求和约束
- 执行时以设计文档为主要参考，必要时可查阅需求文档澄清歧义

**阶段4：编码实现**
- 按执行计划逐步完成开发任务
- 遵循项目既有的代码风格和约定
- 编写必要的单元测试和集成测试
- 如需引入新依赖，需说明理由并与用户确认
- 遇到设计变更时，回到阶段2更新设计文档

**阶段5：测试与验收**
- 根据需求文档和设计文档验证实现
- 确保所有测试通过
- 如不满足需求，制定新计划，从阶段3循环执行

**阶段6：总结**
- 项目完成后，收集本次开发中的新规则和经验，更新到本文档对应章节
- 更新 `docs/plans/debt-tracker.md` 记录遗留的技术债务

**异常流程与回退机制**：
- **编码阶段发现需求不明确**：暂停编码，记录疑点，与用户确认后再继续，必要时回退到阶段1补充需求
- **编码阶段发现设计缺陷**：暂停编码，回退到阶段2更新设计文档，经用户确认后重新进入阶段3调整计划
- **测试阶段发现设计不合理**：记录问题，回退到阶段2修订设计，更新计划后重新执行
- **紧急 hotfix**：可跳过阶段1-3，直接编码修复，事后补充文档记录

---

## 🔍 多角色文档评审

关键文档定稿前，或遇到重要决策时，**应主动建议**用户启用多角色 subagent 并行评审（经用户同意后执行）。**角色组合随评审对象调整，不固定**；默认建议：

- **需求文档 / 产品决策**：产品经理、软件架构师、用户
- **设计文档 / 技术方案**：软件架构师、资深软件工程师、资深测试工程师
- **执行计划 / 测试方案**：资深软件工程师、资深测试工程师、软件架构师

角色职责须互斥，避免视角重叠稀释价值。评审要求：
- 客观批判、不溢美，只报真实问题；技术评审须读源码求证，不凭空猜测代码行为
- **三方收敛的问题优先级最高**——跨视角一致通常意味着真问题
- 评审后须汇总：收敛项与单方发现项分开，并区分"进编码前必修"、"同阶段应补"、"可后置"

---

## 🚫 禁止事项

- **禁止**在未确认需求的情况下开始编码实现
- **禁止**在目录中存放不属于该目录主题的内容（如非需求文档不得放入 `specs/`）
- **禁止**删除或覆盖需求文档中的历史记录和变更历史
- **禁止**需求变更后未更新需求文档就开始实施
- **禁止**跳过需求确认直接开始编码
- **禁止**流程每个阶段完成后未获用户确认就进入下一阶段
- **禁止**需求变更后未等用户确认就修改设计文档或代码
- **禁止**设计文档变更后未等用户确认就修改代码

---

## 文件维护规则

- 需求变更时以追加方式更新需求文档，并记录变更时间
- 只有用户明确要求重构时才重构需求文档
- 支持多个设计方案并行，通过文件名区分
- 文档头部应包含修订记录，记录每次变更的日期、变更人、变更摘要
- 文档内容过期时，标注过期标记并注明替代文档位置，不直接删除
- 执行计划完成后，从 `active/` 移动到 `completed/`，并在文件中记录完成日期

---

## 编码规范

详细的编码规则参见 [CODINGRULES.md](./CODINGRULES.md)，包括：测试规范、Bug 修复规范、字面量与常量规范。

---

## 任务完成检查

- **文档更新评估**：每个任务完成后，必须评估是否需要更新以下文档：`CLAUDE.md`、`docs/specs.md`、`docs/design/`、`docs/architecture.md`（涉及数据协议/完整性语义的代码改动**必须**同步修订）、`README.md`、`CHANGELOG.md`。如需更新，在提交代码前完成
- **强制测试**：编码任务完成后，必须执行单元测试和功能测试（真机或模拟器验证），测试通过后才能提交