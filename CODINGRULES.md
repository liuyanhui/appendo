# CODINGRULE.md - 编码规范

本文件定义项目的编码规则，所有代码编写和修改都必须遵循。

---

## 测试规范

- 测试文件放置在 `tests/` 目录，目录结构与 `src/` 对应
- 单元测试覆盖核心业务逻辑，最低覆盖率目标：60%
- 测试文件命名：`test_<模块名>.<ext>` 或 `<模块名>.test.<ext>`
- 测试内容应包括：正常路径、边界条件、异常输入
- 修复 bug 时，必须先编写能复现 bug 的测试，再编写修复代码
- 测试框架和工具的选择需与用户确认

---

## Bug 修复规范

- **禁止**未经分析就直接修改代码。必须先向用户说明：问题根因、修复方案、影响范围
- **禁止**方案未经用户确认就开始编码
- 编码完成后，必须编写/补充单元测试并运行，确认测试全部通过后才能交付

---

## 字面量与常量规范

- **禁止**在代码中使用硬编码字面量（字符串、颜色值、数字等）。必须提取为常量，集中在合适的对象中管理：
  - 颜色值统一放到 `AppColors` 对象
  - 文件格式、日期格式等统一放到 `MarkdownFormatter` 对象
  - 文件名、路径模式等统一放到对应的 Repository 或工具类中
  - 多处使用的相同值，提取为共享常量，避免重复定义
- 新增字面量时，先检查项目中是否已有对应常量，优先复用
