# 执行计划报告 — 四角色评审问题修复批次（v1.2.2）

> 日期：2026-08-31　计划版本：v1（用户已确认结构：先必修代码项+测试，后文档完善）

## 状态

**VALIDATED**（静态校验一次通过：12 任务、id 唯一、seq 1–12、依赖无环、每任务 ≥3 条可核验标准）

## 范围与布局

- 单文档布局：`./task-list.yaml`（12 任务 ≤ 15 阈值）
- **Phase 1（seq 1–8）必修代码项**：TD-016 回归修复（复现测试先行 + 纯函数化）、TD-018 死规格对齐、TD-017 清空兜底阻塞式、TD-020 测试盲区（并发锁/重复时间戳/死函数/超长文案）、五项定向真机验收、v1.2.2 收尾
- **Phase 2（seq 9–12）文档完善**：决策卡点（trim/allowBackup/迁移/贪睡四项，含预推荐）→ 降级后果核实 → 按拍板执行 → README 用户文档批次
- 范围外：Phase B（TD-007/008）、文档缺陷 A 类（已当场修复）

## 假设清单（执行时可回溯）

1. 用户指令"先修复必须项并通过各类测试，然后完善文档"= 两阶段结构；决策类不阻塞 Phase 1
2. TD-017 按预推荐"阻塞式"（归档失败阻止清空）纳入必修；验收时可回调交互
3. 4 个产品决策（trim/allowBackup/迁移/贪睡）集中 T-decide 一次拍板；预推荐：去 trim、拥抱备份、做迁移、贪睡定位取舍——均可在拍板时推翻
4. 合并发版 **v1.2.2**（versionCode 8）
5. 验收 = JVM 单测 + 构建 + 一次集中真机定向 session（五项）
6. 计划执行完毕后归档至 `docs/plans/completed/`

## 校验结果

| 维度 | 结果 |
|---|---|
| 完整性 | ✅ TD-016~020 全覆盖 + 4 决策 + 定向验收 + README 披露批次 |
| 可执行性 | ✅ 全部动词开头、约束到文件级、每任务 3–4 条 yes/no 标准 |
| 依赖正确性 | ✅ 机器校验通过（T-verify 依赖修复先行；T-readme 依赖决策与核实） |
| 读规模安全 | ✅ 唯一调查任务 T-dgchk 限定 ≤3 个历史版本 |
| 格式规则 | ✅ PyYAML 解析通过 |

## 已知边界

- T-verify 五项真机验收依赖用户配合（含一次重启、一次 SAF 崩溃模拟）
- T-exec 的实际范围取决于 T-decide 拍板结果，计划中以其 4 项决策为界
- atomicWrite rename-fallback 分支测试未单列任务（需注入式重构，成本高于收益，记 backlog）
